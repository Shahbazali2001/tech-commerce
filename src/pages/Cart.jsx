import React from 'react'
import CartCard from '../components/CartCard'
import useTitle from '../hooks/useTitle'

const Cart = () => {
  useTitle('Cart')
  return (
    <div>Cart</div>
  )
}

export default Cart